# Models Directory

This directory stores model checkpoint references, pretrained base weights, and documentation on how to download or export fine-tuned models (`best.pt`).
Binary weight files (`*.pt`, `*.onnx`) are excluded from Git commits per `.gitignore`.
